<% @students.each do |student| %>
  <p class='student_<%= student.id %>'>
    <%= link_to "#{student.name}", student_path(student) %>
    <%= link_to "Delete", student_path(student), method: 'delete' %>
  </p>
<% end %>
